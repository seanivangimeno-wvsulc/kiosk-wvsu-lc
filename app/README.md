# WVSU-LC Kiosk Application (Production App)

This directory is designated for the final production code, build scripts, and environment setups for the kiosk application.

## Structure
- Root configuration files are currently at the project root for local development.
- Build artifacts are output to `dist/`.
- Production bundle configuration can be set up here.
